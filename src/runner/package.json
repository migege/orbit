{
  "name": "@orbit/runner",
  "version": "0.1.0",
  "private": true,
  "description": "Orbit runner — registers a machine and drives Claude Code for assigned tasks",
  "bin": {
    "orbit": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@orbit/shared": "*"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "typescript": "^5.5.4"
  }
}
